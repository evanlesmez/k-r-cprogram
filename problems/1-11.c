// How would you test the word count program? What kinds of input are most
// likely to uncover bugs if there are any
//
//NOTE: evan - I would throw different input at it probably with test .txt files piped in.
// I would focus on boundary areas like extremely long inputs that might overflow each integer counter.
// Also potentially each special character used and as well the written out form of each character or special characters with extra slashes or other special chracters like the "%" sign.
//
int main() {
    return 0;
}
